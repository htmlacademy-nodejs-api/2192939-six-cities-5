export const IMAGE_PREVIEW_MAX_LENGTH = 256;
export const NUMBER_OF_IMAGES = 6;

export enum TitleLength {
  min = 10,
  max = 100,
}

export enum DescriptionLength {
  min = 20,
  max = 1024,
}

export enum BedroomsValue {
  min = 1,
  max = 8,
}

export enum MaxAdultsValue {
  min = 1,
  max = 10,
}

export enum PriceValue {
  min = 100,
  max = 100000,
}
